# Paid PixelLab readiness after v1 production lock

Status: not authorized / not run.

Ready pieces:

- Current canonical Kalev runtime sheets are backed up.
- Canonical sheets hash-match backups after local candidate tests.
- Baseline validation passes.
- V1 mini-sprite identity lock exists and validates as a static idle-front candidate.
- V1 portrait art is preserved separately for dialogue/card use.
- Dry-run previously showed the expected Kalev locomotion paid surface: 8 jobs, templates `breathing-idle` and `walking`, directions south/west/north/east.

Still required before paid generation:

1. PixelLab credential must be present in the actual execution shell without printing the value.
2. Existing PixelLab character rotations should be refreshed/reviewed if credentialed status allows it without generation spend.
3. User must explicitly authorize the exact paid scope: one Kalev locomotion probe, expected 8 jobs, using existing character ID.

Do not infer paid authorization from this local execution pass.
